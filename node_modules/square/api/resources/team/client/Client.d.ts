import type { BaseClientOptions, BaseRequestOptions } from "../../../../BaseClient";
import { type NormalizedClientOptionsWithAuth } from "../../../../BaseClient";
import * as core from "../../../../core";
import type * as Square from "../../../index";
export declare namespace TeamClient {
    type Options = BaseClientOptions;
    interface RequestOptions extends BaseRequestOptions {
    }
}
export declare class TeamClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<TeamClient.Options>;
    constructor(options?: TeamClient.Options);
    /**
     * Lists jobs in a seller account. Results are sorted by title in ascending order.
     *
     * @param {Square.ListJobsRequest} request
     * @param {TeamClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @example
     *     await client.team.listJobs({
     *         cursor: "cursor"
     *     })
     */
    listJobs(request?: Square.ListJobsRequest, requestOptions?: TeamClient.RequestOptions): core.HttpResponsePromise<Square.ListJobsResponse>;
    private __listJobs;
    /**
     * Creates a job in a seller account. A job defines a title and tip eligibility. Note that
     * compensation is defined in a [job assignment](entity:JobAssignment) in a team member's wage setting.
     *
     * @param {Square.CreateJobRequest} request
     * @param {TeamClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @example
     *     await client.team.createJob({
     *         job: {
     *             title: "Cashier",
     *             isTipEligible: true
     *         },
     *         idempotencyKey: "idempotency-key-0"
     *     })
     */
    createJob(request: Square.CreateJobRequest, requestOptions?: TeamClient.RequestOptions): core.HttpResponsePromise<Square.CreateJobResponse>;
    private __createJob;
    /**
     * Retrieves a specified job.
     *
     * @param {Square.RetrieveJobRequest} request
     * @param {TeamClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @example
     *     await client.team.retrieveJob({
     *         jobId: "job_id"
     *     })
     */
    retrieveJob(request: Square.RetrieveJobRequest, requestOptions?: TeamClient.RequestOptions): core.HttpResponsePromise<Square.RetrieveJobResponse>;
    private __retrieveJob;
    /**
     * Updates the title or tip eligibility of a job. Changes to the title propagate to all
     * `JobAssignment`, `Shift`, and `TeamMemberWage` objects that reference the job ID. Changes to
     * tip eligibility propagate to all `TeamMemberWage` objects that reference the job ID.
     *
     * @param {Square.UpdateJobRequest} request
     * @param {TeamClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @example
     *     await client.team.updateJob({
     *         jobId: "job_id",
     *         job: {
     *             title: "Cashier 1",
     *             isTipEligible: true
     *         }
     *     })
     */
    updateJob(request: Square.UpdateJobRequest, requestOptions?: TeamClient.RequestOptions): core.HttpResponsePromise<Square.UpdateJobResponse>;
    private __updateJob;
}
