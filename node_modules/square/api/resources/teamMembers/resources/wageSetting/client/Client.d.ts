import type { BaseClientOptions, BaseRequestOptions } from "../../../../../../BaseClient";
import { type NormalizedClientOptionsWithAuth } from "../../../../../../BaseClient";
import * as core from "../../../../../../core";
import type * as Square from "../../../../../index";
export declare namespace WageSettingClient {
    type Options = BaseClientOptions;
    interface RequestOptions extends BaseRequestOptions {
    }
}
export declare class WageSettingClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<WageSettingClient.Options>;
    constructor(options?: WageSettingClient.Options);
    /**
     * Retrieves a `WageSetting` object for a team member specified
     * by `TeamMember.id`. For more information, see
     * [Troubleshooting the Team API](https://developer.squareup.com/docs/team/troubleshooting#retrievewagesetting).
     *
     * Square recommends using [RetrieveTeamMember](api-endpoint:Team-RetrieveTeamMember) or [SearchTeamMembers](api-endpoint:Team-SearchTeamMembers)
     * to get this information directly from the `TeamMember.wage_setting` field.
     *
     * @param {Square.teamMembers.GetWageSettingRequest} request
     * @param {WageSettingClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @example
     *     await client.teamMembers.wageSetting.get({
     *         teamMemberId: "team_member_id"
     *     })
     */
    get(request: Square.teamMembers.GetWageSettingRequest, requestOptions?: WageSettingClient.RequestOptions): core.HttpResponsePromise<Square.GetWageSettingResponse>;
    private __get;
    /**
     * Creates or updates a `WageSetting` object. The object is created if a
     * `WageSetting` with the specified `team_member_id` doesn't exist. Otherwise,
     * it fully replaces the `WageSetting` object for the team member.
     * The `WageSetting` is returned on a successful update. For more information, see
     * [Troubleshooting the Team API](https://developer.squareup.com/docs/team/troubleshooting#create-or-update-a-wage-setting).
     *
     * Square recommends using [CreateTeamMember](api-endpoint:Team-CreateTeamMember) or [UpdateTeamMember](api-endpoint:Team-UpdateTeamMember)
     * to manage the `TeamMember.wage_setting` field directly.
     *
     * @param {Square.teamMembers.UpdateWageSettingRequest} request
     * @param {WageSettingClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @example
     *     await client.teamMembers.wageSetting.update({
     *         teamMemberId: "team_member_id",
     *         wageSetting: {
     *             jobAssignments: [{
     *                     jobTitle: "Manager",
     *                     payType: "SALARY",
     *                     annualRate: {
     *                         amount: BigInt("3000000"),
     *                         currency: "USD"
     *                     },
     *                     weeklyHours: 40
     *                 }, {
     *                     jobTitle: "Cashier",
     *                     payType: "HOURLY",
     *                     hourlyRate: {
     *                         amount: BigInt("2000"),
     *                         currency: "USD"
     *                     }
     *                 }],
     *             isOvertimeExempt: true
     *         }
     *     })
     */
    update(request: Square.teamMembers.UpdateWageSettingRequest, requestOptions?: WageSettingClient.RequestOptions): core.HttpResponsePromise<Square.UpdateWageSettingResponse>;
    private __update;
}
