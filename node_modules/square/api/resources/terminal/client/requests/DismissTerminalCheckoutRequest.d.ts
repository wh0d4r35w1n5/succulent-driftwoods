/**
 * @example
 *     {
 *         checkoutId: "checkout_id"
 *     }
 */
export interface DismissTerminalCheckoutRequest {
    /** Unique ID for the `TerminalCheckout` associated with the checkout to be dismissed. */
    checkoutId: string;
}
