import type { BaseClientOptions, BaseRequestOptions } from "../../../../../../BaseClient";
import { type NormalizedClientOptionsWithAuth } from "../../../../../../BaseClient";
import * as core from "../../../../../../core";
import type * as Square from "../../../../../index";
export declare namespace CardsClient {
    type Options = BaseClientOptions;
    interface RequestOptions extends BaseRequestOptions {
    }
}
export declare class CardsClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<CardsClient.Options>;
    constructor(options?: CardsClient.Options);
    /**
     * @deprecated
     *
     * Adds a card on file to an existing customer.
     *
     * As with charges, calls to `CreateCustomerCard` are idempotent. Multiple
     * calls with the same card nonce return the same card record that was created
     * with the provided nonce during the _first_ call.
     *
     * @param {Square.customers.CreateCustomerCardRequest} request
     * @param {CardsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @example
     *     await client.customers.cards.create({
     *         customerId: "customer_id",
     *         cardNonce: "YOUR_CARD_NONCE",
     *         billingAddress: {
     *             addressLine1: "500 Electric Ave",
     *             addressLine2: "Suite 600",
     *             locality: "New York",
     *             administrativeDistrictLevel1: "NY",
     *             postalCode: "10003",
     *             country: "US"
     *         },
     *         cardholderName: "Amelia Earhart"
     *     })
     */
    create(request: Square.customers.CreateCustomerCardRequest, requestOptions?: CardsClient.RequestOptions): core.HttpResponsePromise<Square.CreateCustomerCardResponse>;
    private __create;
    /**
     * @deprecated
     *
     * Removes a card on file from a customer.
     *
     * @param {Square.customers.DeleteCardsRequest} request
     * @param {CardsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @example
     *     await client.customers.cards.delete({
     *         customerId: "customer_id",
     *         cardId: "card_id"
     *     })
     */
    delete(request: Square.customers.DeleteCardsRequest, requestOptions?: CardsClient.RequestOptions): core.HttpResponsePromise<Square.DeleteCustomerCardResponse>;
    private __delete;
}
