/**
 * @example
 *     {
 *         rewardId: "reward_id"
 *     }
 */
export interface GetRewardsRequest {
    /** The ID of the [loyalty reward](entity:LoyaltyReward) to retrieve. */
    rewardId: string;
}
