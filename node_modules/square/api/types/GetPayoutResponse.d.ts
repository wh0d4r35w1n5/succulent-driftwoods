import type * as Square from "../index";
export interface GetPayoutResponse {
    /** The requested payout. */
    payout?: Square.Payout;
    /** Information about errors encountered during the request. */
    errors?: Square.Error_[];
}
