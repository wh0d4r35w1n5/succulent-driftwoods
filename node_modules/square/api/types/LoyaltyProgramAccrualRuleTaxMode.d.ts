/**
 * Indicates how taxes should be treated when calculating the purchase amount used for loyalty points accrual.
 * This setting applies only to `SPEND` accrual rules or `VISIT` accrual rules that have a minimum spend requirement.
 */
export declare const LoyaltyProgramAccrualRuleTaxMode: {
    readonly BeforeTax: "BEFORE_TAX";
    readonly AfterTax: "AFTER_TAX";
};
export type LoyaltyProgramAccrualRuleTaxMode = (typeof LoyaltyProgramAccrualRuleTaxMode)[keyof typeof LoyaltyProgramAccrualRuleTaxMode];
