import type * as Square from "../index";
export interface TerminalCheckoutQuery {
    /** Options for filtering returned `TerminalCheckout` objects. */
    filter?: Square.TerminalCheckoutQueryFilter;
    /** Option for sorting returned `TerminalCheckout` objects. */
    sort?: Square.TerminalCheckoutQuerySort;
}
